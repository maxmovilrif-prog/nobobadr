# Nubo Express — PRD

## Problema original
Marketplace multiservicio tipo Glovo para España ("Nubo Express"): delivery de comida/paquetería, ride-sharing (NuboRide), dropshipping y reservas de viajes por afiliados. Stack: FastAPI + React + MongoDB. Idioma del usuario: **Español** (a veces Darija). Responder siempre en español.

## Arquitectura
- backend/server.py — rutas /api, WebSockets de tracking, dropshipping, afiliados, búsqueda IA, admin
- frontend/src — React + Tailwind + Shadcn, i18next (7 idiomas), PWA
- DB: MongoDB (Motor), IDs UUID Pydantic. NO migrar a PostgreSQL.

## Integraciones
- Stripe (pagos) — sk_test_emergent
- Google Maps (placeholder key) — el Admin map usa **Leaflet/OpenStreetMap** (sin key)
- Emergent LLM Key (búsqueda IA) — openai gpt-4o-mini

## Implementado (Jun 2026)
- ✅ **Módulo de CONTABILIDAD nativo (verificado 100% — iteration_21: backend 18/18 pytest + frontend E2E).** Adaptado al stack real (Pydantic v2, UUID, auth admin existente, prefijo `/api`, datetime UTC ISO, helper `db_error`) — NO se usaron los archivos del ZIP (Pydantic v1/Vite/ObjectId), que quedan inactivos como referencia. Endpoints `/api/accounting/*` (solo admin → 403): `cash/balance`, `cash/in`, `cash/out` (valida saldo → 400), `transactions` (GET con filtros+paginación, POST manual), `transactions/summary` (ingresos/gastos/neto por moneda + breakdown), `payroll` (nóminas de couriers EUR+MAD desde pedidos delivered, reutiliza `_compute_earnings`, estado pagado/pendiente), `export/transactions` (CSV). Conversión MAD→EUR centralizada (0.092). Colecciones nuevas: `transactions`, `audit_logs` (auditoría ligera). Frontend: `pages/admin/ContabilidadSection.jsx` (CRA, lucide, tema Emerald, usa AuthContext) con tarjetas de caja MAD/EUR + balance neto, tabla de transacciones con filtro por tipo, diálogo de entrada/salida de caja, tabla de nóminas y export CSV. Integrado como nueva sección `accounting` en `SidebarAdmin` (icono Calculator) + `AdminLayout` + `AdminDashboard` (bloque `section-accounting`). Tests: `/app/backend/tests/test_accounting.py`.
- 📦 **Archivos del sistema admin/contabilidad importado (ZIP) colocados en el repo pero INACTIVOS** (incompatibles con el stack: Pydantic v1, ObjectId, Vite `import.meta.env`, hash SHA-256, stubs): `backend/{models,controllers,routes}/*.py`, `frontend/src/hooks/useAdminData.js`, `frontend/src/views/{SuperAdminDashboard,AccountingPanel}.jsx`, `frontend/src/components/SidebarAdmin.system.jsx`. Se usan solo como ESPECIFICACIÓN; el módulo de contabilidad se reimplementó nativo.
- ✅ **Refactor de UI del Panel de Control Admin con SIDEBAR (verificado 100% — iteration_20).** Nuevos archivos: `components/SidebarAdmin.jsx` (navegación lateral oscura Nubo, colapsable + drawer móvil), `layouts/AdminLayout.jsx` (sidebar + topbar sticky con título por sección + slot de acciones, responsive), `pages/admin/OverviewSection.jsx` (sección "Resumen" con 4 KPIs reales de `/admin/stats`, banda de pedidos en cola de `/admin/pending-orders` y tabla de recientes de `/admin/orders`). `pages/AdminDashboard.js` cableado: estado `section`, las secciones (Resumen, Mapa en vivo, Pedidos, Finanzas, Zonas operativas, Historial) se conmutan con el atributo `hidden` (el mapa Leaflet NO se desmonta; `mapRef.invalidateSize()` al volver a 'Mapa en vivo' → sin recuadro gris). Toda la funcionalidad previa preservada y verificada: finanzas mark-paid↔pendiente, CityManager add/delete, historial+export CSV, filtros de pedidos, asignación manual. Login admin (3 campos) intacto. Responsive móvil OK (hamburguesa + drawer).
- ✅ **Mini-mapa de seguimiento en vivo del pedido exprés (reutilizando `/track`).** Nuevo endpoint público `GET /api/public/orders/{order_id}/tracking` (sin login) que devuelve origen/destino, estado, repartidor y su ubicación en vivo de un pedido exprés. `PublicTracking.js` ahora resuelve pedidos REALES por id (además de los demo ORD-XXXX): mapea estado, convierte la tarifa a MAD base para el toggle MAD/EUR, muestra "🐝 Asignando repartidor…" (clave `searchingDriver` en los 7 idiomas) mientras no hay Abeja, y hace **polling cada 15s** para refrescar ubicación/estado. Tras pagar, `OrderSuccess` muestra el botón "Seguir mi pedido en vivo" (`track-express-btn`) que abre `/track?order=<id>` (solo para pedidos exprés; `payments/status` y `payments/create-checkout` devuelven `order_id`/`order_type`). Verificado: el endpoint devuelve el pedido real con Abeja asignada y ubicación; `/track` renderiza ID, estado, Madrid Centro→Atocha y coste 81,52 د.م ≈ 7,50 € sin romperse (el recuadro del mapa queda gris por el bloqueo de Google Maps del usuario — ErrorBoundary activo). El path completo Stripe→OrderSuccess→track no se reprobó visualmente (requiere pago real), pero los campos backend están verificados.
- ✅ **Endpoint interno admin `POST /api/admin/orders/{order_id}/mark-paid`** (solo admin → 403): marca pagado sin Stripe y dispara el auto-despacho. Verificado E2E: exprés Madrid→Atocha → auto-asignado a "Abeja Madrid" a 0.0 km.
- ✅ **Pedido Exprés A→B completo HASTA EL PAGO + auto-despacho desde el origen.** (1) `placeExpressOrder` (CustomerDashboard) ahora encadena `POST /api/orders/express` → `POST /api/payments/create-checkout` → redirige al **checkout de Stripe** (verificado: redirige a checkout.stripe.com con el importe correcto €12.50). (2) `create_checkout_session` usa la moneda del pedido (`currency` EUR/MAD, default eur). (3) `auto_assign_order` usa las coordenadas reales del **origen** (`origin_lat/origin_lng`) para pedidos exprés (no el centro de la ciudad), de modo que al confirmarse el pago se asigna al instante la Abeja más cercana al punto de recogida. Verificado E2E (iteration_19): backend pytest 10/10 + test directo de auto-despacho exprés PASS.
- ✅ **Gestor de Ciudades / Zonas operativas en el Panel Admin (CRUD).** Componente `CityManager.jsx` montado en AdminDashboard (tarjeta `city-manager-card`): alta (nombre, país ES/MA, lat, lng), edición (diálogo) y borrado (confirmación). Backend: `POST/PATCH/DELETE /api/admin/cities` (solo admin → 403; nombre duplicado → 400; borrado bloqueado si hay pedidos activos en la ciudad → 400). Campo `country` añadido al modelo City. Verificado E2E (iteration_19, CRUD completo).
- ✅ **Campo de cuenta bancaria (IBAN) en el alta de negocio.** `bank_account` añadido a `Business`/`BusinessCreate` (backend, se persiste y se devuelve) e input `business-bank-input` en el diálogo de crear negocio (BusinessDashboard) para el seguimiento del pago de comisiones. Verificado E2E (iteration_19).
- ✅ **Deployment readiness:** health check PASS. Limpiado `.gitignore` (eliminadas decenas de patrones `*.env` duplicados que impedían incluir los `.env` en el deploy). NOTA producción: la MongoDB gestionada usa el nombre `nuboexpress-nobo_algeciras` (prefijo automático) y muestra warnings de autorización en el seed/índices del deploy — es config del lado producción, no del código (el preview funciona con `DB_NAME='glovo_algeciras'`).
- ✅ **Pedidos Exprés A→B (mensajería punto a punto) — flujo completo.** Nuevo endpoint `POST /api/orders/express` (`server.py` ~L500) que crea un pedido con `order_type='express'`, `business_id=null`, `items=[]`, `total_amount=fee`, origen/destino (lat/lng), `vehicle_type`, `distance_km`, `eta_mins`, `currency` y resuelve `city_name` desde `origin_city_id`. Solo `customer` (driver/business → 403). El frontend `/cotizar` (`DeliveryQuote.handleOrderThis`) ahora guarda en `nubo_quote_prefill` también las coordenadas (originLat/originLng/destLat/destLng) + originCityId. En `CustomerDashboard` el banner (`quote-prefill-banner`) muestra un botón **"Pedir esta entrega"** (`quote-prefill-order-btn`) que llama a `placeExpressOrder` → `POST /api/orders/express`, muestra toast de éxito, cierra el banner (quotePrefill→null) y refresca pedidos. Verificado E2E (iteration_18): backend 7/7 pytest (`/app/backend/tests/test_express_orders.py`), frontend banner+creación+cierre+refresh. PENDIENTE usuario: habilitar Google Routes API para el cálculo natural en /cotizar (la inyección de localStorage emula el resultado).
- ✅ **Botón "Pedir esta entrega" → checkout con datos precargados.** En el resultado de `/cotizar`, el botón (`quote-order-btn`) guarda el presupuesto (origen, destino, vehículo, tarifa, distancia, ETA) en `localStorage` (`nubo_quote_prefill`) y lleva a `/auth` (sin sesión) o `/dashboard` (con sesión). El `CustomerDashboard` lee el prefill, muestra un banner (`quote-prefill-banner`) con la solicitud y **precarga la dirección de entrega (destino) + la ciudad** en el formulario de pedido, luego limpia la clave. Verificado E2E (iteration_17): banner correcto, dirección='Madrid', city-select con el id real, botón Cerrar funciona; ninguna página en blanco.
- ✅ **Acceso visible a la calculadora de tarifa.** Botón "Calcular Tarifa" (icono calculadora) en el **navbar** de la landing (`data-testid nav-quote-btn`) y enlace en el **hero** (`hero-quote-link`), ambos navegan a `/cotizar`. Texto traducido en los 7 idiomas (`landing.cta_quote`). Verificado en preview (ambos visibles y enlazados).
- ✅ **Calculadora de tarifa real `/cotizar` (pública).** Página con selección de origen/destino (ciudades ES+MA agrupadas por país), selector de vehículo con iconos PNG de marca (moto/coche/bici), toggle de divisa EUR/MAD e i18next (7 idiomas, RTL). Llama a `POST /api/v1/calculate-delivery` (Routes API) y muestra tarifa, distancia y ETA. Backend: endpoint público `GET /api/public/cities` (con `country`), conversión de divisa EUR↔MAD en el cálculo, campo `country` añadido a las ciudades sembradas. Ruta `/cotizar` en App.js. Verificado: UI renderiza con iconos PNG; el cálculo está pendiente de que el usuario habilite **Routes API** en Google Cloud.
- ✅ **Iconos PNG de marca por vehículo + endpoint de tarifa/ETA.** (1) Creados 4 iconos PNG (`frontend/src/assets/icons/`: moto-bee, car-bee, bike-bee, truck-bee — círculo verde Nubo + silueta + detalle abeja) y `getVehicleIcon(type)` en `PublicTracking.js` con guarda segura (`window.google?.maps?.Size`); el marcador del repartidor usa el PNG según `vehicle_type` (motorcycle/car/bicycle/truck). (2) Nuevo endpoint público `POST /api/v1/calculate-delivery` (FastAPI) que usa **Google Routes API** (sustituye Distance Matrix legacy) para distancia/tiempo reales y calcula `delivery_fee` (base+km por tipo) y `adjusted_eta_mins` (factor de velocidad por vehículo); clave en `backend/.env` `GOOGLE_MAPS_API_KEY` (no hardcodeada). Verificado: endpoint devuelve error limpio porque falta habilitar Routes API en el proyecto Google. Datos demo de `/track` mantenidos (ORD-4821 camión, 4822 coche, 4823 moto, 4824 bici). PENDIENTE usuario (Google Cloud): habilitar **Maps JavaScript API + Directions API + Routes API** y referrers.
- ✅ **Fix crash pantalla en blanco en `/track`:** al cargar un pedido con las APIs de Google bloqueadas (`ApiTargetBlockedMapError`), `new window.google.maps.Size(...)` lanzaba excepción en el render del `<Marker>` y tumbaba toda la página (blank screen en build de producción). Solucionado con (1) guarda `window.google?.maps?.Size` antes de construir el icono, (2) `MapErrorBoundary` que aísla los fallos de render del mapa, (3) try/catch en `drawRoute`, (4) fallback `mapUnavailable` (7 idiomas). Ahora si el mapa falla, el pedido, coste y botón de compartir se siguen mostrando. Verificado en preview (ORD-4821 renderiza completo sin crash).
- ✅ **Botón "Compartir" (WhatsApp) en seguimiento privado (`OrderTracking.js`):** abre `wa.me` con el enlace público `/track?order=<orderId>` y un mensaje. Verificado frontend E2E (iteration_16): visible, verde WhatsApp, clicable y resto de la página intacto.
- ✅ **`/track`: compartir por WhatsApp + marcador dinámico por vehículo + enlace directo.** Botón "Compartir seguimiento" (verde WhatsApp) que abre `wa.me` con un mensaje localizado + enlace `https://<dominio>/track?order=ORD-XXXX`. La página auto-carga el pedido si se entra con `?order=` (vía `useSearchParams`) y sincroniza la URL al buscar. El marcador del mapa cambia según `vehicle_type`: camión/furgoneta (truck), coche (car) y moto (motorcycle), cada uno con su silueta SVG. Datos demo: ORD-4821 (truck), ORD-4822 (car), ORD-4823 (motorcycle). Traducciones `share`/`shareMessage` en los 7 idiomas. Verificado frontend E2E (iteration_15). Mapa pendiente de que el usuario habilite Maps JavaScript API + Directions API en Google Cloud (ApiTargetBlockedMapError). Deployment health check: PASS.
- ✅ **Página pública de seguimiento `/track` (bilingüe Marruecos↔España):** nueva página sin login con búsqueda por nº de pedido (datos DEMO: ORD-4821, ORD-4822), mapa de Google con ruta (Directions) + marcador de la Abeja + A/B, panel lateral con estado, origen/destino, distancia/duración, coste con conversión **MAD↔EUR** (toggle) y tarjeta del conductor. Integrada con **i18next (7 idiomas)** vía clave `publicTracking` en los 7 archivos de traducción + `LanguageSelector` global (RTL en árabe); diseño Tailwind/shadcn tema verde. Ruta `/track` en App.js. Clave Google Maps movida a `REACT_APP_GOOGLE_MAPS_API_KEY` (misma para el mapa actual y el nuevo). Verificado frontend E2E (iteration_14): búsqueda demo, error si no existe, toggle divisa, cambio de idioma ES/EN/AR. PENDIENTE (lado usuario): el mapa da `ApiTargetBlockedMapError` hasta habilitar Maps JavaScript API + Directions API y los referrers en Google Cloud Console. Datos DEMO — conectar al backend real más adelante (los pedidos reales no guardan coords origen/destino aún).
- ✅ **Historial de pagos visible (registro contable):** tarjeta "Historial de pagos" en el panel admin que lista los pagos registrados (repartidor, periodo, entregas, importe, tasa, fecha de pago) desde `GET /admin/finances/payouts` (enriquecido con `driver_name`). Se actualiza al marcar/desmarcar un pago. Verificado backend (payouts devuelve nombre, importe, periodo, paid_at).
- ✅ **Pagos "Pagado/Pendiente" por repartidor (cierre de ciclo, P1):** en el panel Admin de Finanzas, cada Abeja muestra un badge de estado (Pagado/Pendiente) y un botón para alternar. Endpoints `POST /admin/finances/mark-paid` (recalcula el importe en servidor y guarda instantánea en `driver_payouts`, clave única driver+periodo), `POST /admin/finances/mark-pending` (revierte), `GET /admin/finances/payouts` (historial). El `summary` anota `payment_status`/`paid_at` por fila y añade `paid_earnings`/`pending_earnings` a los totales; CSV incluye columna `estado_pago`. El estado persiste por periodo (start/end/rate). Verificado: backend e2e (pending→paid→pending, totales, payouts) + frontend E2E (toggle, totales, toast, persistencia tras reload).
- ✅ **Aviso en tiempo real a la Abeja (WebSocket + sonido + vibración):** canal `/ws/driver/{driver_id}` (`DriverNotifier`). Al asignar un pedido (manual o auto-despacho), `notify_driver_new_order` envía `{type:'new_order', business_name, delivery_address, city_name, total_amount, ...}`. El `DriverDashboard` mantiene una conexión persistente con keepalive + reconexión automática y, al recibir `new_order`, reproduce un doble "beep" (Web Audio API, sin archivos), vibra el móvil (`navigator.vibrate`), muestra un toast y refresca pedidos al instante (sin recargar). Entrega WS verificada e2e.
- ✅ **Despacho automático (auto-asignación al instante):** en cuanto un pedido pasa a `payment_status='paid'` (confirmación de pago `get_payment_status` y `stripe_webhook`), `auto_assign_order` asigna automáticamente la Abeja más cercana usando el centro de la ciudad del pedido como punto de recogida y respetando el radio de zona (`CITY_ZONE_RADIUS_KM=10`). Reutiliza el núcleo atómico `_atomic_claim_nearest` (mismo loop anti-carrera). Silencioso: sin ciudad o sin Abejas libres, el pedido queda en la cola. Verificado e2e (pedido Madrid → Abeja Madrid, status 'busy').
- ✅ **Asignación atómica del repartidor (anti-condición-de-carrera):** `POST /orders/{id}/assign-nearest` usa bucle de hasta 3 intentos (`ASSIGN_MAX_ATTEMPTS=3`), excluye Abejas ya descartadas y reclama al candidato con `find_one_and_update` atómico (`is_available True→False`, `status='busy'`); también reclama el pedido atómicamente (`driver_id None→driver`). Helper `release_driver` libera a la Abeja (`is_available=True`, `status='available'`) al entregar/cancelar (`update_order_status`) o devolver a la cola. Campo `status` en User (available|busy|offline). Verificado 19/19 (test_atomic_assignment.py + test_assign_nearest.py).
- ✅ **España + Marruecos:** 8 ciudades activas (🇲🇦 Tánger, Casablanca, Meknes, Nador · 🇪🇸 Algeciras, Madrid, Barcelona, Málaga) con seed idempotente. Textos de región actualizados a "España y Marruecos" en los 7 idiomas + Landing/TravelBooking; mapa admin re-centrado para ver ambos países.
- ✅ **Panel admin "Todos los pedidos":** `GET /api/admin/orders` (filtros por estado y ciudad) + tabla en el panel (id, negocio, ciudad, repartidor, estado, importe, fecha). Verificado (backend curl + selectores frontend).
- ✅ **Ciudades + Geofencing:** colección `cities` (Tánger, Casablanca, Meknes, Nador con coords). `GET /api/cities`. El cliente elige ciudad al pedir (`order.city_id`/`city_name`). El repartidor solo ve pedidos **de su ciudad**, detectada por GPS (≤10 km del centro); fuera de zona ve solo pedidos sin ciudad. Badge "Tu zona" en el panel del repartidor. Verificado 9/9.
- ✅ **Pagos por repartidor (comisiones):** panel admin con rango de fechas, % comisión configurable, tabla por Abeja (entregas/ingresos/comisión) + totales + CSV. Endpoints `/admin/finances/summary`, `/report/{driver_id}`, `/export`. `delivered_at` se registra al entregar. Verificado 13/13.
- ✅ **Fix seguridad:** `PATCH /orders/{id}/status` ahora solo admin / repartidor asignado / dueño del negocio (antes cualquiera podía marcar "delivered").
- ✅ **Historial: filtros + exportación CSV** (`GET /api/admin/assignment-history` con filtros action/driver/fechas + `/export` CSV). Card con selects de acción/repartidor, rango de fechas, Limpiar y "Exportar CSV". Verificado 14/14.
- ✅ **Stealth redirect:** un usuario no-admin que entre a `/admin-nubo` va a `/dashboard`; solo visitantes anónimos ven el login admin. El código secreto se valida 100% en servidor (nunca se expone al navegador).
- ✅ **Retorno automático a la cola** (Abeja "No disponible" → sus pedidos pre-entrega vuelven a pendientes) + devolución manual (`/orders/{id}/return-to-queue`).
- ✅ **Historial de asignaciones** (colección `assignment_history`): registra assigned/auto_returned/returned con quién, qué, cuándo, distancia y motivo.
- ✅ **Logística geoespacial 2dsphere:** `/drivers/nearest`, `/orders/{id}/assign-nearest`; botón "Asignar más cercano" con confirmación + el mapa vuela y resalta la Abeja elegida.
- ✅ **Inicialización de esquema MongoDB** (colecciones + índices) al arranque.
- ✅ **Portal Admin separado/oculto** (`/admin-nubo`): login oscuro email+contraseña+código secreto, bloqueo anti fuerza bruta, robots/noindex; `/auth` rechaza admin.
- ✅ Mensajes de error de Auth específicos · GPS automático del repartidor · Búsqueda IA · Admin Live Map (Leaflet).
- (Sesiones previas) Páginas legales, WebSockets tracking, 7 idiomas, viajes afiliados, NuboRide, dropshipping.

## Producción
- Dominio en vivo: https://noboexpress.com (los cambios requieren Redeploy).

## Credenciales de prueba
Ver /app/memory/test_credentials.md

## Herramientas internas (admin)
- `POST /api/admin/orders/{order_id}/mark-paid` (solo admin): marca un pedido como pagado SIN Stripe y dispara el auto-despacho. Útil para probar el flujo courier completo end-to-end. Verificado: pedido exprés Madrid→Atocha → auto-asignado a "Abeja Madrid" a 0.0 km. (403 para no-admin; 400 si ya está pagado.)

## Backlog / Próximas tareas
- P1: Campo de cuenta bancaria en BusinessDashboard para seguimiento de comisiones (acordado, pendiente).
- P2: Guía publicación Google Play / App Store (PWA → TWA/Capacitor).
- P2: Verificar cambio de dominio a noboexpress.com (support agent, pendiente verificación usuario).
- Refactor: server.py >700 líneas → dividir en routers (auth, businesses, orders, admin, search).

## Estado de pruebas
- Backend: 10/10 pytest PASS (/app/backend/tests/test_nubo_features.py) — iteration_2.
- Frontend: Búsqueda IA + Admin map 100% (iteration_2, iteration_3).
